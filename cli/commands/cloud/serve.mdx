---
title: "cloud serve"
description: "Serve your local instance via Flipt Cloud"
---

```
flipt cloud serve [flags]
```

## Prerequisites

Before you get started with Flipt Hybrid Cloud, you need to have the following:

1. An active Flipt Hybrid Cloud account. If you don't have an account yet, [sign up here](https://tally.so/r/mV0zWM) and we'll get you set up.
2. Enable the `cloud` experimental feature in the Flipt configuration file. You can enable the `cloud` feature by adding the following configuration to your `flipt.yaml` file:

```yaml
experimental:
  cloud:
    enabled: true
```

### Options

```
  -h, --help   help for edit
```

### Inherited Options

```
  -u, --url string   Flipt Cloud URL (default "https://flipt.cloud")
```

### More Info

See the [Getting Started](/cloud/guides/getting-started) guide for more information on setting up Flipt Hybrid Cloud and using the `flipt cloud serve` command.
